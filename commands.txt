# Data can be found at "data/"
# Available models: lstm, gru, bilst, transformer, itransformer

# Create a dataset
# python scripts/create_dataset.py 

# All 5 models on your dataset
python benchmark.py --data <dataset-path>

# Only specific models
python benchmark.py --data <dataset-path> --model <model-name1> <model-name2> <model-name3> ...

# Faster test run (fewer epochs)
python benchmark.py --data <dataset-path> --epochs 10

# Trains one model and saves weights and scalers to disk.
python train.py --data <dataset-path> --model <model-name>
python train.py --data <dataset-path> --model <model-name> --epochs 100
python train.py --data <dataset-path> --model <model-name> --d_model 256